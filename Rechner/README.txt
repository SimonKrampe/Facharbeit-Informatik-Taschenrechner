Komma werden im Rechner als '.' eingegeben.
Die Laufzeitberechnung lässt sich über die Optionen von rechner.jar aufrufen, welche oben in der Toolbar des Programmes sind. Die Dateien mit den Laufzeiten
werden dort gespeichert wo rechner.jar gespeichert ist.

controlDisplay.py und RGB1602.py werden von rechnerRaspiListener.jar verwendet - diese Jar funktioniert nur auf dem Raspberry.